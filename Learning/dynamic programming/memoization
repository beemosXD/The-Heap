memoization is used to stop recursion of previously generated keys 
the memo should be = to the non memoized child calls and be unique to the parameters it can be generated like key = m +  "," n
and called like memo[key]